Listen now I want you to listen to me carefully to what I'm saying because I'm going to do something very dangerous, right, something very, very crazy, so that it will improve my entire platform completely. Right now, when my AI creates websites, it actually creates the website for you. You can actually choose something called a database and it actually automatically connects you to a database within my website. The database feature is inside the chat input on the website and it actually automatically creates a database for you with the supabase website API. Okay, now it actually uses the server and also creates a login page for you, creating an account, all these things with the supabase server. Now I want the login to the website not to be with their server. It will be saved inside the server as well, but it's not actually to create a login page and a basic account. I really want to define login and account creation pages for it within the code, not within the format file, but within the code file. The display will be automatic. Only if you use the database feature, I think it's possible to create it without actually connecting it in the codes. I'll explain to you, I want the entire login and account creation system to create pages for me within my site that actually won't work with the files that he would have created. You'll explain this to him later, but it's actually inside the files. Let's say a user wants to create a site and he's using the server. So actually, when inside the site when the AI ​​created it, you'll actually see the login and account creation buttons. When you click on it, it doesn't take you inside the pages of the site itself that the AI ​​created for you. It actually takes you to the login and account logout page. It actually opens another site for you, which is actually my site, and there it opens a new login and account logout page. It depends on what you're logging in to. You create these pages for me. These are pages just for logging in and creating an account for these sites. They actually use my Clerk account. I actually use the site on Clerk. So from what I understand, I can actually connect one Clerk account that people will actually use inside the sites they build with the automatic database, which will actually be users. Now, each user is going to work like this: userId (optional) Default) or organization / projectId (recommended) 💡 Simple summary One Clerk project ✅ Many sites use it ✅ Each user creates their own account on each site ✅ You separate them using identifiers or organizations ✅
So that it doesn't conflict with each other users
You will choose what is most recommended if it is a user ID

And actually after a user has logged in, it actually creates the new ID for that user and actually he logs in to that site, it simply makes him go back to the same site that he actually tried to log in to and actually logs out the account logs in and everything he does is saved on that new account that he created Okay, what I want to do is basically this I want the login and logout page to actually have the buttons that I have Look at the login and account creation page that I currently have on the site on my site, create a copy of them for me but that it will only be suitable for the chats themselves and actually in the prompt emphasize to him how to do it I want you to actually update the api files and the things that are needed so that this actually happens What we want to do here is say You have a tab in the database panel called Users. I want it to actually show the users who are connected to, say, the same site as it works now. Just try to do it for me.